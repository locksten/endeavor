mutation DoPositiveHabit($doPositiveHabitId: ID!) {
    doPositiveHabit(id: $doPositiveHabitId) {
        id
        title
        difficulty
        positiveCount
        negativeCount
        createdAt
    }
}