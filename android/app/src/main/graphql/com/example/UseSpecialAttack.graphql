mutation UseSpecialAttack($multiplier: Float!) {
    useSpecialAttack(multiplier: $multiplier) {
        id
        creatureHitpoints
        partyMembers {
            id
            hitpoints
            energy
            maxHitpoints
            maxEnergy
            level
        }
    }
}