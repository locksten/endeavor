mutation CreateTask($createTaskTitle: String!, $createTaskDifficulty: Int!) {
  createTask(title: $createTaskTitle, difficulty: $createTaskDifficulty) {
    id
    isCompleted
    title
    difficulty
    createdAt
  }
}