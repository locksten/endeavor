mutation UpdateDaily($updateDailyInput: UpdateDailyInput!) {
  updateDaily(updateDailyInput: $updateDailyInput) {
    id
    isCompleted
    lastCompletionDate
    title
    difficulty
    createdAt
  }
}