mutation CreateDaily($createDailyInput: CreateDailyInput!) {
  createDaily(createDailyInput: $createDailyInput) {
    id
    isCompleted
    lastCompletionDate
    title
    difficulty
    createdAt
  }
}