query Vitals {
  me {
    id
    user {
      id
      username
      hitpoints
      maxEnergy
      maxHitpoints
      energy
      level
      experienceInCurrentLevel
      experienceForNexLevel
    }
  }
}